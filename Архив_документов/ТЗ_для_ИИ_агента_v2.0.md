# ТЗ для ИИ-агента v2.0

**Система:** LabPro — система прослеживаемости клеточных культур и учёта инвентаря

**Версия:** 2.0
**Дата:** 30.01.2026
**Обновлено:** 30.01.2026 (v1.3 — готовые среды, заявки, QC, дашборд)

---

## 1. КОНТЕКСТ ПРОЕКТА

### 1.1 Назначение системы
Система для лабораторного ведения клеточных культур с полной прослеживаемостью:
- Учёт доноров и донаций
- Создание и ведение культур и их веток (лотов)
- Учёт контейнеров (чашки/флаконы/криовиалы) с QR-этикетками
- Журнал манипуляций и метрик
- Учёт инвентаря (среды, добавки, энзимы, пластик)
- Учёт оборудования и позиций хранения
- Формирование документов (Рабочий лист, Паспорт культуры)
- **v1.3:** Готовые питательные среды, заявки от заказчиков, QC-тесты, дашборд с задачами

### 1.2 Технологический стек
- **Backend:** Node.js + Express / Python + FastAPI
- **Database:** PostgreSQL
- **Frontend:** React (Web) / PWA (мобильные)
- **Auth:** JWT + bcrypt (web), биометрия (PWA)

### 1.3 Ключевые сущности (иерархия)
```
Donor (D-0001)
  └─ Donation (DN-0001) → привязан к donor_id
      └─ Culture (CT-0001) → привязан к donation_id, опционально order_id
          ├─ Lot L1 (P0) → passage=0, parent_lot_id=NULL
          │   ├─ Container CT-0001-L1-P0-FL-001
          │   └─ Container CT-0001-L1-P0-DSH-001
          └─ Lot L2 (P1) → passage=1, parent_lot_id=L1 (split)
              └─ Container CT-0001-L2-P1-FL-001
```

**ВАЖНО:** Работа оператора ВСЕГДА начинается с Культуры, внутри — от Лотов. Контейнеры — это физические объекты для сканирования QR.

---

## 2. ГЛОССАРИЙ

| Термин | Определение | Пример |
|--------|-------------|--------|
| **Донор** | Физическое лицо, от которого получена ткань | D-0001 |
| **Донация** | Факт получения ткани от донора | DN-0001 |
| **Культура** | Корневой объект, привязанный к одной донации | CT-0001 |
| **Лот (ветка)** | Независимая ветка истории внутри культуры | L1, L2 |
| **Контейнер** | Физическая единица: посуда или криовиала | CT-0001-L1-P2-FL-003 |
| **Банк** | Набор криовиал, созданных одной операцией заморозки | MCB (Master Bank), WCB (Working Bank) |
| **Пассаж (P)** | Номер пассажа, увеличивается при каждом пересеве | P0, P1, P2 |
| **Split** | Ветвление лота при операции на части контейнеров | L1 → L2 |
| **PD** | Population Doubling = log2(N_final / N_initial) | 1.5 |
| **CPD** | Cumulative PD = сумма PD всех пассажей от создания культуры | 12.3 |
| **FEFO** | First Expired, First Out — принцип списания инвентаря | - |
| **Готовая среда** | Приготовленная питательная среда из компонентов | RM-0001 |
| **Заявка** | Запрос от заказчика на MCB или выдачу клеток | ORD-0001 |
| **QC-тест** | Тест контроля качества (микоплазма, стерильность) | MYCOPLASMA |
| **Задача** | Повторяющееся напоминание в дашборде | Осмотр каждые 3 дня |
| **Уведомление** | Одноразовое сообщение в дашборде | QC-тест готов |

---

## 3. МОДЕЛЬ ДАННЫХ (PostgreSQL)

### 3.0 СПРАВОЧНИКИ

Все справочники должны быть предзаполнены данными перед началом работы системы.

#### 3.0.1 Типы тканей (dict_tissue_types)
```sql
INSERT INTO dict_tissue_types (code, name_ru) VALUES
('SKIN', 'Кожа'),
('CARTILAGE', 'Хрящ'),
('BONE', 'Кость'),
('MUSCLE', 'Мышечная ткань'),
('NERVE', 'Нервная ткань'),
('OTHER', 'Другое');
```

#### 3.0.2 Типы клеток (dict_cell_types)
```sql
-- Связаны с типами тканей через tissue_type_id
INSERT INTO dict_cell_types (tissue_type_id, code, name_ru) VALUES
-- Кожа
(1, 'FB', 'Фибробласты'),
(1, 'KC', 'Кератиноциты'),
-- Хрящ
(2, 'CC', 'Хондроциты'),
-- Кость
(3, 'OC', 'Остеоциты'),
(3, 'MSC', 'Мезенхимальные стволовые клетки'),
-- Мышечная ткань
(4, 'MC', 'Миоциты'),
-- Нервная ткань
(5, 'NC', 'Нейроциты'),
(5, 'SC', 'Шванновские клетки');
```

#### 3.0.3 Типы контейнеров (dict_container_types)
```sql
INSERT INTO dict_container_types (name_ru, kind, nominal_volume_ml, area_cm2) VALUES
('Чашка 35мм', 'DISH', 2.0, 9.6),
('Чашка 60мм', 'DISH', 5.0, 28.3),
('Чашка 100мм', 'DISH', 10.0, 78.5),
('Чашка 150мм', 'DISH', 20.0, 176.7),
('Флакон T-25', 'FLASK', 5.0, 25.0),
('Флакон T-75', 'FLASK', 15.0, 75.0),
('Флакон T-175', 'FLASK', 35.0, 175.0),
('Криовиала 1.0мл', 'VIAL', 1.0, NULL),
('Криовиала 2.0мл', 'VIAL', 2.0, NULL),
('Криовиала 4.0мл', 'VIAL', 4.0, NULL);
```

#### 3.0.4 Морфология (dict_morphology)
```sql
INSERT INTO dict_morphology (code, name_ru) VALUES
('SPINDLE', 'Веретенообразная'),
('POLYGONAL', 'Полигональная'),
('FIBROBLAST', 'Фибробластоподобная'),
('ROUND', 'Округлая'),
('MIXED', 'Смешанная');
```

#### 3.0.5 Типы контаминации (dict_contamination_types)
```sql
INSERT INTO dict_contamination_types (code, name_ru) VALUES
('NONE', 'Нет контаминации'),
('BACTERIAL', 'Бактериальная'),
('FUNGAL', 'Грибковая'),
('MYCOPLASMA', 'Микоплазма'),
('VIRAL', 'Вирусная');
```

#### 3.0.6 Причины утилизации (dict_disposal_reasons)
```sql
INSERT INTO dict_disposal_reasons (code, name_ru) VALUES
('CONTAMINATION', 'Контаминация'),
('AGING', 'Старение культуры'),
('EXPERIMENT_END', 'Окончание эксперимента'),
('QUALITY_FAIL', 'Неудовлетворительное качество'),
('OTHER', 'Другое');
```

#### 3.0.7 Способы получения культуры (dict_culture_obtainment_methods)
```sql
INSERT INTO dict_culture_obtainment_methods (code, name_ru, tissue_form) VALUES
('ENZYMATIC', 'Ферментативный (трипсин/коллагеназа)', 'SOLID'),
('EXPLANT', 'Эксплантный метод', 'SOLID'),
('GRADIENT', 'Градиентное центрифугирование', 'LIQUID'),
('ISOTONIC', 'Изотонический лизис', 'LIQUID');
-- tissue_form: 'SOLID' или 'LIQUID' — определяет доступные методы
```

#### 3.0.8 Категории готовых сред (dict_media_categories) — v1.3
```sql
INSERT INTO dict_media_categories (code, name_ru) VALUES
('BASE_MEDIUM', 'Базовая среда'),      -- DMEM, RPMI
('SERUM', 'Сыворотка'),                -- FBS, BCS
('ENZYME', 'Ферменты'),                -- трипсин, коллагеназа
('ADDITIVE', 'Добавки'),               -- антибиотики, глутамин
('BUFFER', 'Буфер'),                   -- PBS, HEPES
('OTHER', 'Иное');
```

#### 3.0.9 Типы оборудования (dict_equipment_types)
```sql
INSERT INTO dict_equipment_types (code, name_ru, temperature_example) VALUES
('INCUBATOR', 'Инкубатор', '+37°C'),
('FREEZER', 'Морозильник -80°C', '-80°C'),
('CRYO', 'Криохранилище', '-196°C'),
('LAMINAR', 'Ламинарный шкаф', '+20-25°C'),
('CENTRIFUGE', 'Центрифуга', '+20-25°C'),
('MICROSCOPE', 'Микроскоп', '+20-25°C'),
('BIOSAFETY', 'Бокс биологической безопасности', '+20-25°C'),
('REFRIGERATOR', 'Холодильник', '+4°C');
```

#### 3.0.10 Типы QC-тестов (dict_qc_test_types) — v1.3
```sql
INSERT INTO dict_qc_test_types (code, name_ru) VALUES
('MYCOPLASMA', 'Микоплазма'),
('STERILITY', 'Стерильность'),
('LAL', 'Эндотоксин (LAL)'),
('PHENOTYPE', 'Фенотип'),
('INFECTION', 'Инфекции');
```

#### 3.0.11 Типы заявок (dict_order_types) — v1.3
```sql
INSERT INTO dict_order_types (code, name_ru) VALUES
('MCB_CREATION', 'Создание мастер-банка'),
('CELL_DELIVERY', 'Выдача клеток');
```

#### 3.0.12 Классы чистоты помещений (rooms)
```sql
INSERT INTO rooms (name_ru, cleanliness_class) VALUES
('Чистая зона 1', 'ISO5'),
('Чистая зона 2', 'ISO7'),
('Чистая зона 3', 'ISO8'),
('Лаборатория общего назначения', NULL);
```

### 3.1 Пользователи
```sql
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    login VARCHAR(100) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    full_name VARCHAR(200) NOT NULL,
    role VARCHAR(50) NOT NULL, -- 'OPERATOR', 'ADMIN', 'QA'
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT NOW()
);
```

### 3.2 Доноры и Донорации
```sql
CREATE TABLE donors (
    id SERIAL PRIMARY KEY,
    donor_code VARCHAR(50) UNIQUE NOT NULL, -- 'D-0001'
    fio VARCHAR(300) NOT NULL,
    sex VARCHAR(10),
    birth_date DATE,
    contacts TEXT,
    note TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE donations (
    id SERIAL PRIMARY KEY,
    donation_code VARCHAR(50) UNIQUE NOT NULL, -- 'DN-0001'
    donor_id INTEGER REFERENCES donors(id),
    tissue_type_id INTEGER REFERENCES dict_tissue_types(id),
    collected_at TIMESTAMP NOT NULL,
    
    -- v1.3: форма ткани
    tissue_form VARCHAR(20) NOT NULL, -- 'LIQUID' или 'SOLID'
    tissue_volume_ml DECIMAL(10,2),   -- для жидких
    tissue_weight_g DECIMAL(10,2),    -- для солидных
    
    -- v1.3: документы
    consent_received BOOLEAN DEFAULT FALSE,
    consent_document BYTEA,           -- скан согласия
    contract_number VARCHAR(100),     -- номер договора
    contract_date DATE,               -- дата договора
    
    inf_hiv BOOLEAN, inf_hbv BOOLEAN, inf_hcv BOOLEAN, inf_syphilis BOOLEAN,
    
    -- v1.3: статус донации
    status VARCHAR(20) NOT NULL DEFAULT 'QUARANTINE', 
    -- 'QUARANTINE' — тесты в процессе, выдача запрещена
    -- 'APPROVED' — готово к использованию
    -- 'REJECTED' — отклонено
    
    note TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);
```

### 3.3 Культуры и Лоты
```sql
CREATE TABLE cultures (
    id SERIAL PRIMARY KEY,
    culture_code VARCHAR(50) UNIQUE NOT NULL, -- 'CT-0001'
    donation_id INTEGER REFERENCES donations(id),
    cell_type_id INTEGER REFERENCES dict_cell_types(id),
    
    -- v1.3: связь с заявкой
    order_id INTEGER REFERENCES customer_orders(id), -- опционально
    
    status VARCHAR(50) NOT NULL, -- 'ACTIVE', 'CLOSED', 'DISPOSED'
    created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE culture_lots (
    id SERIAL PRIMARY KEY,
    culture_id INTEGER REFERENCES cultures(id),
    lot_index INTEGER NOT NULL, -- 1, 2, 3... (формат L1, L2)
    parent_lot_id INTEGER REFERENCES culture_lots(id), -- для split
    passage INTEGER NOT NULL DEFAULT 0, -- P0, P1, P2...
    status VARCHAR(50) NOT NULL, -- 'ACTIVE', 'CLOSED', 'DISPOSED'
    created_at TIMESTAMP DEFAULT NOW(),
    closed_at TIMESTAMP,
    closed_by_id INTEGER REFERENCES users(id)
);
```

### 3.4 Контейнеры и Размещения
```sql
CREATE TABLE containers (
    id SERIAL PRIMARY KEY,
    container_code VARCHAR(100) UNIQUE NOT NULL, -- 'CT-0001-L1-P2-FL-003'
    culture_lot_id INTEGER REFERENCES culture_lots(id),
    container_type_id INTEGER REFERENCES dict_container_types(id),
    status VARCHAR(50) NOT NULL, -- 'ACTIVE', 'IN_STORAGE', 'CONSUMED', 'DISPOSED'
    created_at TIMESTAMP DEFAULT NOW(),
    closed_at TIMESTAMP
);

CREATE TABLE equipment_positions (
    id SERIAL PRIMARY KEY,
    equipment_id INTEGER NOT NULL,
    parent_position_id INTEGER REFERENCES equipment_positions(id),
    position_code_ru VARCHAR(100) NOT NULL,
    temperature_conditions_ru VARCHAR(100),
    capacity INTEGER,
    position_usage VARCHAR(20) DEFAULT 'WORK', -- 'WORK' или 'STORAGE'
    current_usage INTEGER DEFAULT 0
);

CREATE TABLE container_placements (
    id SERIAL PRIMARY KEY,
    container_id INTEGER REFERENCES containers(id),
    equipment_position_id INTEGER REFERENCES equipment_positions(id),
    placed_at TIMESTAMP DEFAULT NOW(),
    removed_at TIMESTAMP,
    event_id INTEGER
);
```

### 3.5 Банки
```sql
CREATE TABLE banks (
    id SERIAL PRIMARY KEY,
    culture_lot_id INTEGER REFERENCES culture_lots(id),
    bank_type VARCHAR(50) NOT NULL, -- 'MCB' или 'WCB'
    created_at TIMESTAMP DEFAULT NOW(),
    cells_per_ml DECIMAL(15,2),
    suspension_volume_ml DECIMAL(10,2),
    viability_percent DECIMAL(5,2),
    volume_per_vial_ml DECIMAL(10,2),
    freeze_type VARCHAR(50), -- 'PROGRAMMED', 'MANUAL_80'
    freeze_medium_id INTEGER,
    freeze_started_at TIMESTAMP,
    freeze_ended_at TIMESTAMP,
    placed_in_cryo_at TIMESTAMP
);

CREATE TABLE bank_vials (
    id SERIAL PRIMARY KEY,
    bank_id INTEGER REFERENCES banks(id),
    container_id INTEGER REFERENCES containers(id),
    vial_number INTEGER NOT NULL,
    passage INTEGER NOT NULL,
    cells_per_ml DECIMAL(15,2),
    volume_per_vial_ml DECIMAL(10,2),
    cells_per_vial DECIMAL(15,2)
);
```

### 3.6 Инвентарь
```sql
CREATE TABLE inventory_items (
    id SERIAL PRIMARY KEY,
    category VARCHAR(50) NOT NULL, -- 'MEDIUM', 'ADDITIVE', 'ENZYME', 'PLASTIC'
    name_ru VARCHAR(200) NOT NULL,
    unit VARCHAR(20) NOT NULL, -- 'ml', 'ml', 'ml', 'pcs'
    is_active BOOLEAN DEFAULT TRUE
);

CREATE TABLE inventory_lots (
    id SERIAL PRIMARY KEY,
    item_id INTEGER REFERENCES inventory_items(id),
    lot_number VARCHAR(100) NOT NULL,
    expires_at TIMESTAMP,
    received_at TIMESTAMP DEFAULT NOW(),
    opened_at TIMESTAMP,
    qty_remaining DECIMAL(15,3) NOT NULL,
    status VARCHAR(50) NOT NULL, -- 'ACTIVE', 'EMPTY', 'EXPIRED'
    equipment_position_id INTEGER REFERENCES equipment_positions(id), -- v1.3
    note TEXT
);

CREATE TABLE inventory_movements (
    id SERIAL PRIMARY KEY,
    inventory_lot_id INTEGER REFERENCES inventory_lots(id),
    movement_type VARCHAR(50) NOT NULL, -- 'RECEIVE', 'OPEN', 'USE', 'ADJUST', 'DISCARD'
    qty DECIMAL(15,3) NOT NULL,
    moved_at TIMESTAMP DEFAULT NOW(),
    event_id INTEGER REFERENCES events(id),
    note TEXT
);
```

### 3.7 Готовые питательные среды — v1.3
```sql
CREATE TABLE ready_media (
    id SERIAL PRIMARY KEY,
    code VARCHAR(50) UNIQUE NOT NULL, -- 'RM-0001'
    name VARCHAR(200) NOT NULL, -- 'Среда для культивирования 10% FBS'
    category_id INTEGER REFERENCES dict_media_categories(id),
    sterilization_method VARCHAR(50), -- 'FILTER', 'AUTOCLAVE'
    prepared_at TIMESTAMP DEFAULT NOW(),
    expires_at TIMESTAMP NOT NULL,
    qty_ml DECIMAL(15,2) NOT NULL,
    storage_position_id INTEGER REFERENCES equipment_positions(id),
    status VARCHAR(20) NOT NULL, -- 'QUARANTINE', 'ACTIVE', 'CONSUMED'
    note TEXT,
    created_by_id INTEGER REFERENCES users(id),
    created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE ready_media_components (
    id SERIAL PRIMARY KEY,
    ready_media_id INTEGER REFERENCES ready_media(id),
    inventory_lot_id INTEGER REFERENCES inventory_lots(id),
    qty_ml DECIMAL(15,2) NOT NULL,
    final_concentration DECIMAL(10,4) -- авторасчёт
);

CREATE TABLE event_ready_media (
    -- v1.3: использование готовых сред в событиях
    event_id INTEGER REFERENCES events(id),
    ready_media_id INTEGER REFERENCES ready_media(id),
    qty_ml DECIMAL(15,2) NOT NULL,
    PRIMARY KEY (event_id, ready_media_id)
);
```

### 3.8 Заявки от заказчиков — v1.3
```sql
CREATE TABLE customer_orders (
    id SERIAL PRIMARY KEY,
    order_code VARCHAR(50) UNIQUE NOT NULL, -- 'ORD-0001'
    customer_name VARCHAR(200) NOT NULL,
    customer_contacts TEXT,
    cell_type_id INTEGER REFERENCES dict_cell_types(id),
    tissue_type_id INTEGER REFERENCES dict_tissue_types(id),
    quantity_required INTEGER NOT NULL, -- количество клеток
    delivery_date DATE NOT NULL,
    order_type VARCHAR(50) NOT NULL, -- 'MCB_CREATION', 'CELL_DELIVERY'
    status VARCHAR(50) NOT NULL, -- 'NEW', 'IN_PROGRESS', 'COMPLETED', 'CANCELLED'
    created_at TIMESTAMP DEFAULT NOW(),
    notes TEXT
);
```

### 3.9 QC-тесты — v1.3
```sql
CREATE TABLE qc_tests (
    id SERIAL PRIMARY KEY,
    test_type VARCHAR(50) NOT NULL, -- 'MYCOPLASMA', 'STERILITY', 'LAL', 'PHENOTYPE', 'INFECTION'
    culture_id INTEGER REFERENCES cultures(id),
    culture_lot_id INTEGER REFERENCES culture_lots(id),
    bank_id INTEGER REFERENCES banks(id),
    performed_at TIMESTAMP,
    result VARCHAR(20), -- 'PENDING', 'PASSED', 'FAILED'
    notes TEXT,
    performed_by_id INTEGER REFERENCES users(id),
    created_at TIMESTAMP DEFAULT NOW()
);
```

### 3.10 Дашборд: Задачи и уведомления — v1.3
```sql
-- Повторяющиеся задачи
CREATE TABLE dashboard_tasks (
    id SERIAL PRIMARY KEY,
    task_type VARCHAR(50) NOT NULL, -- 'INSPECTION', 'FEED', 'FEFO', 'MAINTENANCE', 'VALIDATION'
    entity_type VARCHAR(50), -- 'culture', 'lot', 'equipment', 'inventory'
    entity_id INTEGER,
    due_date TIMESTAMP NOT NULL,
    is_completed BOOLEAN DEFAULT FALSE,
    completed_at TIMESTAMP,
    completed_by_id INTEGER REFERENCES users(id),
    next_due_date TIMESTAMP, -- для повторяющихся задач
    created_at TIMESTAMP DEFAULT NOW()
);

-- Одноразовые уведомления
CREATE TABLE dashboard_notifications (
    id SERIAL PRIMARY KEY,
    notification_type VARCHAR(50) NOT NULL, -- 'QC_READY', 'ORDER_DEADLINE', 'CRITICAL_FEFO', 'EQUIPMENT_ALERT'
    title VARCHAR(200) NOT NULL,
    message TEXT,
    entity_type VARCHAR(50),
    entity_id INTEGER,
    is_read BOOLEAN DEFAULT FALSE,
    read_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT NOW()
);
```

### 3.11 События (журнал)
```sql
CREATE TABLE events (
    id SERIAL PRIMARY KEY,
    event_type VARCHAR(100) NOT NULL, 
    -- 'CULTURE_CREATE', 'INSPECTION', 'FEED', 'PASSAGE', 
    -- 'BANK_FREEZE', 'BANK_THAW', 'PLACE', 'ISSUE', 'DISPOSE',
    -- 'ISOLATION' (v1.3)
    event_at TIMESTAMP DEFAULT NOW(),
    user_id INTEGER REFERENCES users(id) NOT NULL,
    culture_id INTEGER REFERENCES cultures(id),
    culture_lot_id INTEGER REFERENCES culture_lots(id),
    equipment_id INTEGER,
    note TEXT,
    corrects_event_id INTEGER REFERENCES events(id)
);

CREATE TABLE event_containers (
    event_id INTEGER REFERENCES events(id),
    container_id INTEGER REFERENCES containers(id),
    role VARCHAR(50) NOT NULL, -- 'source', 'result', 'observed'
    PRIMARY KEY (event_id, container_id)
);

CREATE TABLE event_metrics (
    event_id INTEGER PRIMARY KEY REFERENCES events(id),
    confluence_percent DECIMAL(5,2),
    morphology_id INTEGER REFERENCES dict_morphology(id),
    contamination_flag BOOLEAN,
    contamination_note TEXT,
    cells_per_ml DECIMAL(15,2),
    suspension_volume_ml DECIMAL(10,2),
    viability_percent DECIMAL(5,2)
);

CREATE TABLE event_inventory (
    event_id INTEGER REFERENCES events(id),
    inventory_lot_id INTEGER REFERENCES inventory_lots(id),
    qty DECIMAL(15,3) NOT NULL,
    unit VARCHAR(20) NOT NULL,
    PRIMARY KEY (event_id, inventory_lot_id)
);

CREATE TABLE lot_splits (
    id SERIAL PRIMARY KEY,
    event_id INTEGER REFERENCES events(id),
    source_lot_id INTEGER REFERENCES culture_lots(id),
    new_lot_id INTEGER REFERENCES culture_lots(id)
);

CREATE TABLE event_photos (
    id SERIAL PRIMARY KEY,
    event_id INTEGER REFERENCES events(id),
    content_type VARCHAR(100) NOT NULL,
    file_name VARCHAR(255) NOT NULL,
    bytes BYTEA,
    created_at TIMESTAMP DEFAULT NOW()
);

-- v1.3: Процедура выделения
CREATE TABLE isolation_procedures (
    id SERIAL PRIMARY KEY,
    event_id INTEGER REFERENCES events(id),
    method_code VARCHAR(50) NOT NULL, -- 'ENZYMATIC', 'EXPLANT', 'GRADIENT', 'ISOTONIC'
    dissociation_medium_id INTEGER,   -- для солидных
    wash_medium_id INTEGER,
    gradient_medium_id INTEGER,       -- для жидких
    isotonic_medium_id INTEGER,       -- для жидких
    start_time TIMESTAMP,
    end_time TIMESTAMP,
    duration_minutes INTEGER -- авторасчёт
);
```

### 3.12 Документы
```sql
CREATE TABLE document_versions (
    id SERIAL PRIMARY KEY,
    doc_type VARCHAR(50) NOT NULL, -- 'WORKSHEET', 'CULTURE_PASSPORT'
    culture_id INTEGER REFERENCES cultures(id),
    culture_lot_id INTEGER REFERENCES culture_lots(id),
    created_at TIMESTAMP DEFAULT NOW(),
    user_id INTEGER REFERENCES users(id),
    file_name VARCHAR(255) NOT NULL,
    content_type VARCHAR(100) NOT NULL,
    bytes BYTEA,
    sha256 VARCHAR(64),
    note TEXT
);
```

### 3.13 Оборудование
```sql
CREATE TABLE equipment (
    id SERIAL PRIMARY KEY,
    equipment_code VARCHAR(100) UNIQUE NOT NULL, -- 'EQP-INC-001'
    equipment_type_id INTEGER REFERENCES dict_equipment_types(id),
    manufacturer_code VARCHAR(100),
    manufacturer_name VARCHAR(200),
    model_name VARCHAR(200),
    serial_number VARCHAR(100),
    inventory_number VARCHAR(100),
    room_id INTEGER REFERENCES rooms(id),
    status VARCHAR(50) NOT NULL, -- 'ACTIVE', 'MAINTENANCE', 'BROKEN'
    last_service_at TIMESTAMP,
    next_service_at TIMESTAMP,
    last_validation_at TIMESTAMP,
    next_validation_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT NOW()
);
```

---

## 4. БИЗНЕС-ПРАВИЛА

### 4.1 Принцип работы оператора
Пользователь работает от Культуры, внутри — от Лотов. Любая манипуляция выполняется на всех или части контейнеров выбранного лота.

### 4.2 Неизменяемость журнала
- Записи событий НЕ редактируются и НЕ удаляются
- Изменения состояния выполняются ТОЛЬКО через создание события
- Исправления — через корректирующее событие с `corrects_event_id`

### 4.3 Ветвление (split) лота
**Когда происходит split:**
- Пассаж на части контейнеров
- Заморозка на части контейнеров
- Разморозка (P_vial < P_lot)

**Правила split:**
- Новый лот создаётся с `parent_lot_id = source_lot_id`
- Контейнеры-источники остаются в исходном лоте (статус "Израсходован")
- Новые контейнеры создаются в новом лоте
- lot_index увеличивается: L1 → L2 → L3
- passage наследуется или увеличивается

### 4.4 Разморозка и пассаж — правило P_vial vs P_lot
```javascript
if (P_vial === P_lot) {
    continueInSameLot();
} else if (P_vial < P_lot) {
    createNewLotWithSplit();
} else {
    throw new Error('Нельзя размораживать клетки с пассажем выше текущего');
}
```

### 4.5 Закрытие лота (автоматическое)
Лот закрывается АВТОМАТИЧЕСКИ, когда все его контейнеры переходят в статус "Израсходован" или "Утилизирован".

### 4.6 FEFO
При списании инвентаря партии сортируются по `expires_at` (сначала просроченные, потом ближайшие к просрочке).

**Предупреждение:** Если оператор выбирает не-FEFO партию → показать предупреждение.

### 4.7 Статус донации — v1.3
- **QUARANTINE** — тесты в процессе, выдача запрещена
- **APPROVED** — готово к использованию
- **REJECTED** — отклонено

### 4.8 Размещение контейнеров — v1.3

| Операция | Размещение | Примечание |
|----------|------------|------------|
| Пассаж | Инкубатор (WORK) | **Обязательно** |
| Разморозка | Инкубатор (WORK) | **Обязательно** |
| Заморозка | Криохранилище (STORAGE) | **Обязательно** |
| Выдача | Опционально | Можно пропустить |
| Утилизация | Опционально | Можно пропустить |

### 4.9 Генерация кодов

| Сущность | Формат | Пример |
|----------|--------|--------|
| Донор | D-0001 … D-9999 | D-0001 |
| Донация | DN-0001 … DN-9999 | DN-0001 |
| Культура | CT-0001 … CT-9999 | CT-0001 |
| Контейнер (посуда) | {CULT}-L{ЛОТ}-P{ПАССАЖ}-{ВИД}-{NNN} | CT-0007-L1-P2-FL-003 |
| Контейнер (криовиала) | {CULT}-L{ЛОТ}-{БАНК}-V{NNN} | CT-0007-L1-WCB-V012 |
| Банк | MCB / WCB | MCB, WCB |
| Готовая среда — v1.3 | RM-0001 … RM-9999 | RM-0001 |
| Заявка — v1.3 | ORD-0001 … ORD-9999 | ORD-0001 |
| Оборудование | EQP-{TYPE}-{NNN} | EQP-INC-001 |

### 4.10 QR-форматы — v1.3
```
Контейнер:  CNT:CT-0001-L1-P2-FL-003
Оборудование: EQP:EQP-INC-001
Партия инвентаря: INV:123
Позиция: POS:456
Культура: CULT:CT-0001
Готовая среда: RM:RM-0001
Заявка: ORD:ORD-0001
```

---

## 5. API СПЕЦИФИКАЦИЯ

### 5.1 Аутентификация
См. раздел 5.1 в оригинальном ТЗ.

### 5.2 QR resolve
См. раздел 5.2 в оригинальном ТЗ.

### 5.3 Операции

**Новые операции v1.3:**
```json
POST /api/operations/isolation
{
  "donation_id": 1,
  "cell_type_id": 1,
  "method_code": "ENZYMATIC",  -- или EXPLANT, GRADIENT, ISOTONIC
  "dissociation_medium_id": 10,    -- для солидных
  "wash_medium_id": 11,
  "gradient_medium_id": 12,        -- для жидких (альтернатива)
  "isotonic_medium_id": 13,        -- для жидких (альтернатива)
  "containers": [{"container_type_id": 1, "count": 2}],
  "placement": {"equipment_position_id": 10},
  "start_time": "2026-01-30T10:00:00",
  "end_time": "2026-01-30T11:30:00"
}

POST /api/operations/issue-with-thaw  -- v1.3: выдача с манипуляцией
{
  "vial_container_ids": [200, 201, 202],  -- криовиалы для разморозки
  "wash_medium_id": 50,  -- среда для отмывки (инвентарь или готовая)
  "use_ready_media": false,  -- true = из ready_media
  "metrics": {
    "cells_per_ml": 1800000,
    "suspension_volume_ml": 10,
    "viability_percent": 85
  },
  "packaging": {
    "container_type_id": 1,  -- флакон/пробирка
    "count": 5,
    "volume_per_container_ml": 1.0
  },
  "recipient": "Иванов А.А.",
  "department": "Отдел регенеративной медицины",
  "purpose": "Исследование"
}
```

### 5.4 Готовые среды — v1.3
```json
POST /api/ready-media
{
  "name": "Среда для культивирования 10% FBS",
  "category_id": 1,
  "sterilization_method": "FILTER",
  "expires_at": "2026-02-15T00:00:00",  -- или NULL для авторасчёта
  "qty_ml": 500,
  "storage_position_id": 100,
  "components": [
    {"inventory_lot_id": 10, "qty_ml": 450},  -- DMEM
    {"inventory_lot_id": 11, "qty_ml": 50}    -- FBS
  ]
}

GET /api/ready-media
// Ответ: список с фильтром по статусу (QUARANTINE/ACTIVE/CONSUMED)

PUT /api/ready-media/{id}/status
{
  "status": "ACTIVE"  -- из QUARANTINE в ACTIVE
}

POST /api/ready-media/{id}/print-label
// Возвращает URL этикетки для печати
```

### 5.5 Заявки — v1.3
```json
POST /api/orders
{
  "customer_name": "Институт клеточных технологий",
  "customer_contacts": "ivanov@ict.ru",
  "cell_type_id": 1,
  "tissue_type_id": 1,
  "quantity_required": 50000000,  -- 50 млн клеток
  "delivery_date": "2026-03-01",
  "order_type": "MCB_CREATION",  -- или CELL_DELIVERY
  "notes": "Проект 2026-07"
}

GET /api/orders
// Ответ: список заявок с фильтром по статусу

PUT /api/orders/{id}/status
{
  "status": "IN_PROGRESS"  -- NEW → IN_PROGRESS → COMPLETED/CANCELLED
}
```

### 5.6 QC-тесты — v1.3
```json
POST /api/qc-tests
{
  "test_type": "MYCOPLASMA",
  "culture_id": 1,  -- или culture_lot_id, или bank_id
  "bank_id": null
}

PUT /api/qc-tests/{id}/result
{
  "result": "PASSED",  -- или FAILED
  "notes": "Тест пройден успешно"
}
```

### 5.7 Дашборд — v1.3
```json
GET /api/dashboard/tasks
// Ответ:
{
  "tasks": [
    {
      "id": 1,
      "task_type": "INSPECTION",
      "entity_type": "culture",
      "entity_id": 1,
      "entity_code": "CT-0001",
      "due_date": "2026-02-01T10:00:00",
      "is_completed": false,
      "message": "Осмотр CT-0001 (последний: 27.01)"
    },
    {
      "task_type": "FEED",
      "entity_type": "culture",
      "entity_id": 2,
      "due_date": "2026-01-31T10:00:00",
      "message": "Подкормка CT-0002 (последняя: 26.01)"
    },
    {
      "task_type": "FEFO",
      "entity_type": "inventory",
      "entity_id": 50,
      "due_date": "2026-02-06T00:00:00",
      "message": "FEFO: DMEM/F12 истекает 15.02"
    }
  ]
}

GET /api/dashboard/notifications
// Ответ:
{
  "notifications": [
    {
      "id": 1,
      "notification_type": "QC_READY",
      "title": "QC-тест готов",
      "message": "Тест на микоплазму для CT-0001 пройден",
      "entity_type": "culture",
      "entity_id": 1,
      "is_read": false
    },
    {
      "notification_type": "ORDER_DEADLINE",
      "title": "Заявка скоро дедлайн",
      "message": "Заявка ORD-0001 — срок 05.02",
      "entity_type": "order",
      "entity_id": 1
    }
  ]
}

POST /api/dashboard/tasks/{id}/complete
// Отмечает задачу как выполненную, сбрасывает счётчик

POST /api/dashboard/tasks/{id}/cancel
// Отменяет задачу, сбрасывает счётчик
```

### 5.8 Справочники
```json
GET /api/dicts/tissue-types
GET /api/dicts/cell-types
GET /api/dicts/morphology
GET /api/dicts/contamination-types
GET /api/dicts/disposal-reasons
GET /api/dicts/obtainment-methods
GET /api/dicts/container-types
GET /api/dicts/equipment-types
GET /api/dicts/media-categories  -- v1.3
GET /api/dicts/qc-test-types     -- v1.3
GET /api/dicts/order-types       -- v1.3
```

---

## 6. ОПЕРАЦИИ (ПОШАГОВЫЕ АЛГОРИТМЫ)

### 6.1 Создание культуры

**v1.3: Добавлены поля для методов выделения**
```javascript
{
  donation_id: integer,
  cell_type_id: integer,
  obtainment_method_id: integer,
  
  // v1.3: детали метода
  dissociation_medium_id: integer,    // для ENZYMATIC (солидная)
  wash_medium_id: integer,
  gradient_medium_id: integer,        // для GRADIENT (жидкая)
  isotonic_medium_id: integer,        // для ISOTONIC (жидкая)
  
  // v1.3: время процедуры
  start_time: timestamp,
  end_time: timestamp,
  
  containers: [{container_type_id, count}],
  placement: {equipment_position_id}  // обязательно — инкубатор
}
```

**v1.3: Прогноз клеток**
После создания культуры система рассчитывает ожидаемое количество клеток:
```sql
-- На основе средней плотности и площади контейнеров
SELECT AVG(density_per_cm2) FROM event_metrics 
WHERE cells_per_ml IS NOT NULL AND confluence_percent = 70;

estimated_cells = SUM(container_area_cm2) × density_per_cm2 × confluence_factor;
```

---

### 6.2 Осмотр

**v1.3: Автоматический интервал**
- Задача "Осмотр" создаётся автоматически каждые 3 дня
- При выполнении → создаётся новая задача через 3 дня
- При отмене/пропуске → счётчик сбрасывается

**v1.3: Конфлюэнтность**
Можно ввести общее значение или для каждого контейнера отдельно.

---

### 6.3 Подкормка

**v1.3: Использование готовых сред**
```javascript
{
  lot_id: integer,
  container_ids: [integer],
  
  // Можно использовать партию инвентаря ИЛИ готовую среду
  inventory_lot_id: integer,      // для простой среды
  OR
  ready_media_id: integer,        // для готовой среды (v1.3)
  
  volume_ml: decimal
}
```

---

### 6.4 Пассаж

**v1.3: Среды для снятия клеток**
```javascript
{
  lot_id: integer,
  source_container_ids: [integer],
  
  // v1.3: Среды — из готовых сред или инвентаря
  dissociation_medium_id: integer,  -- трипсин
  wash_medium_id: integer,          -- PBS
  inactivation_medium_id: integer,  -- среда с FBS для инактивации
  
  metrics: {cells_per_ml, suspension_volume_ml, viability_percent},
  result: {container_type_id, count},
  
  // v1.3: время
  start_time: timestamp,
  end_time: timestamp
}
```

**v1.3: Прогноз клеток**
Рассчитывается ожидаемое количество перед выполнением.

---

### 6.5 Создание банка (заморозка)

**v1.3: Обязательные поля**
```javascript
{
  lot_id: integer,
  source_container_ids: [integer],
  bank_type: 'MCB' | 'WCB',
  
  // v1.3: Среды для снятия клеток
  dissociation_medium_id: integer,
  wash_medium_id: integer,
  freeze_medium_id: integer,
  
  // v1.3: время
  freeze_started_at: timestamp,
  freeze_ended_at: timestamp,
  placed_in_cryo_at: timestamp,
  
  volume_per_vial_ml: decimal,
  vials_count: integer,
  metrics: {cells_per_ml, suspension_volume_ml, viability_percent},
  
  placement: {equipment_position_id}  // обязательно — криохранилище
}
```

---

### 6.6 Разморозка

**v1.3: Обязательное размещение**
```javascript
{
  vial_container_id: integer,
  target_lot_id: integer,  // опционально
  wash_medium_id: integer, // среда для отмывки
  use_ready_media: boolean,
  
  metrics: {cells_per_ml, suspension_volume_ml, viability_percent},
  
  placement: {equipment_position_id}  // обязательно — инкубатор
}
```

---

### 6.7 Выдача — два сценария — v1.3

**Сценарий А: Простая выдача (как было)**
```javascript
{
  container_ids: [integer],  // криовиалы
  recipient: string,
  department: string,
  purpose: string,
  expected_return_date: date
}
```

**Сценарий Б: Выдача с манипуляцией (новый)**
```javascript
{
  vial_container_ids: [integer],  // криовиалы для разморозки
  wash_medium_id: integer,
  use_ready_media: boolean,
  
  metrics: {cells_per_ml, suspension_volume_ml, viability_percent},
  
  packaging: {
    container_type_id: integer,  -- флакон/пробирка
    count: integer,
    volume_per_container_ml: decimal
  },
  
  recipient: string,
  department: string,
  purpose: string,
  expected_return_date: date
}
```

---

### 6.8 Процедура выделения (ISOLATION) — v1.3

**Новый тип операции для создания культуры из донации**
```javascript
{
  donation_id: integer,
  cell_type_id: integer,
  method_code: 'ENZYMATIC' | 'EXPLANT' | 'GRADIENT' | 'ISOTONIC',
  
  // Для солидных тканей
  dissociation_medium_id: integer,
  wash_medium_id: integer,
  
  // Для жидких тканей (альтернатива)
  gradient_medium_id: integer,  // или
  isotonic_medium_id: integer,
  
  start_time: timestamp,
  end_time: timestamp,
  
  containers: [{container_type_id, count}],
  placement: {equipment_position_id}
}
```

---

### 6.9 Утилизация

**v1.3: Привязка к QC**
При утилизации проверяется:
- Статус донации != QUARANTINE
- QC-тесты культура/банка не FAILED

---

## 7. ЭКРАНЫ СИСТЕМЫ

### 7.1 Главная панель (Dashboard) — v1.3

**Обновлённый макет:**
```
┌─────────────────────────────────────────────────────────┐
│  LabPro  │  Пользователь: Иванов И.И.  [Выход]          │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─ ЗАДАЧИ ───────────────────────────────────────────┐│
│  │ ☑ Осмотр CT-0001 (последний: 27.01)              ││
│  │ ☑ Подкормка CT-0002 (последняя: 26.01)           ││
│  │ ☑ FEFO: DMEM/F12 истекает 15.02                  ││
│  │ ☑ Обслуживание INC-01 через 15 дней              ││
│  └────────────────────────────────────────────────────┘│
│                                                         │
│  ┌─ УВЕДОМЛЕНИЯ ─────────────────────────────────────┐│
│  │ ⚠ QC: CT-0001 — тест на микоплазму готов        ││
│  │ ⚠ Заявка ORD-0001 — срок 05.02                  ││
│  │ ⚠ Оборудование CRYO-01 — валидация              ││
│  └────────────────────────────────────────────────────┘│
│                                                         │
│  ┌─ СТАТИСТИКА ──────────────────────────────────────┐│
│  │ Всего культур: 5  │  Активных: 3                  ││
│  │ На пассаже P2: 2  │  Банков: 4                    ││
│  └────────────────────────────────────────────────────┘│
│                                                         │
│  ┌─ Быстрые действия ─┐  ┌─ Последние культуры ─┐   ││
│  │ [+ Новая культура] │  │ CT-0001 | Активен   │   ││
│  │ [+ Новая заявка]   │  │ CT-0002 | Активен   │   ││
│  │ [📷 Сканировать QR]│  │ CT-0003 | Активен   │   ││
│  │ [📋 Рабочий лист]  │  └─────────────────────┘   ││
│  │ [📄 Паспорт]       │                            ││
│  └────────────────────┘                            ││
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 7.2 Инвентарь — v1.3

**Добавлен раздел "Готовые среды":**
```
┌─ Готовые среды ─────────────────────────────────────┐ │
│  [+ Создать]  [🔍 Поиск]  [Фильтр: Все ▼]           │ │
│                                                     │ │
│  ┌─ RM-0001 ──────────────────────────────────────┐ │ │
│  │ Среда для культивирования 10% FBS               │ │ │
│  │ Категория: Базовая среда                        │ │ │
│  │ Объём: 500 мл │ Годен до: 15.02.2026            │ │ │
│  │ Статус: АКТИВНА │ Хранение: Холодильник 1      │ │ │
│  │ Состав: DMEM 90% + FBS 10%                      │ │ │
│  │ [📄 Этикетка] [📊 История]                      │ │ │
│  └─────────────────────────────────────────────────┘ │ │
└─────────────────────────────────────────────────────────┘
```

### 7.3 Заявки — v1.3 (новый экран)

```
┌─────────────────────────────────────────────────────┐
│  Заявки  │  [+ Новая заявка]  [🔍 Поиск]            │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌─ ORD-0001 ─────────────────────────────────────┐ │
│  │ Статус: В работе  │  Тип: Создание МБ          │ │
│  │ Заказчик: Институт клеточных технологий        │ │
│  │ Клетки: Фибробласты (FB) │ 50 млн клеток       │ │
│  │ Срок: 01.03.2026                                │ │
│  │ Связанная культура: CT-0001 [→]                │ │
│  └────────────────────────────────────────────────┘ │
│                                                     │
│  ┌─ ORD-0002 ─────────────────────────────────────┐ │
│  │ Статус: Новая  │  Тип: Выдача клеток           │ │
│  │ Заказчик: Иванов А.А.                          │ │
│  │ Клетки: Фибробласты (FB) │ 10 млн клеток       │ │
│  │ Срок: 15.02.2026                                │ │
│  └────────────────────────────────────────────────┘ │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 7.4 QC — v1.3 (новый экран)

```
┌─────────────────────────────────────────────────────┐
│  Контроль качества  │  [+ Новый тест]              │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌─ Ожидающие ───────────────────────────────────┐ ││
│  │ CT-0001 / Микоплазма / 27.01 / —              │ ││
│  │ CT-0001 / Стерильность / 28.01 / —            │ ││
│  └────────────────────────────────────────────────┘│
│                                                     │
│  ┌─ Готовые ─────────────────────────────────────┐ ││
│  │ CT-0001 / Микоплазма / 29.01 / ПРОЙДЕН ✓     │ ││
│  │ CT-0002 / LAL / 29.01 / ПРОЙДЕН ✓            │ ││
│  └────────────────────────────────────────────────┘│
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## 8. ПЕЧАТЬ ЭТИКЕТОК

### 8.1 Этикетка готовой среды — v1.3

```
┌─────────────────────────────────────────────┐
│ ┌─────────────────────────────────────────┐ │
│ │  ██████████████  RM:RM-0001            │ │  ← QR
│ └─────────────────────────────────────────┘ │
│                                             │
│  Среда для культивирования 10% FBS          │
│                                             │
│  Состав: DMEM 90% + FBS 10%                 │
│  Объём: 500 мл                              │
│  Годен до: 15.02.2026                       │
│  Хранение: Холодильник 1 / Полка 3          │
│  Статус: АКТИВНА                            │
│                                             │
│  LabPro | 30.01.2026                        │
└─────────────────────────────────────────────┘
```

---

## 9. CHECKLIST ДЛЯ РАЗРАБОТЧИКА

### Backend
- [ ] Все таблицы из раздела 3
- [ ] Аутентификация (JWT + bcrypt)
- [ ] QR resolve endpoint
- [ ] Все операции из раздела 6 (включая v1.3)
- [ ] Справочники CRUD
- [ ] Готовые среды (CRUD, печать этикеток)
- [ ] Заявки от заказчиков
- [ ] QC-тесты
- [ ] Дашборд: задачи и уведомления
- [ ] Прогноз количества клеток
- [ ] Генерация PDF
- [ ] FEFO-логика
- [ ] Автоматическое закрытие лотов

### Frontend
- [ ] React + PWA
- [ ] Сканирование QR
- [ ] Все экраны из раздела 7 (включая v1.3)
- [ ] Дашборд с задачами и уведомлениями
- [ ] Экран готовых сред
- [ ] Экран заявок
- [ ] Экран QC
- [ ] Биометрия для PWA

### Тестирование
- [ ] Unit-тесты для бизнес-правил
- [ ] Интеграционные тесты для операций
- [ ] E2E-тесты для сценариев

---

**Документ v2.0 обновлён 30.01.2026 (v1.3)**
